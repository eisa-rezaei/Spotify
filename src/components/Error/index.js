import React from "react";
import Layout from "../Layout";
import { StErrorContainer } from "./styles";

const Error = () => {
  return (
    <Layout>
      <StErrorContainer>
        Some thing wrong with URL, please use navigation! -_-
      </StErrorContainer>
    </Layout>
  );
};

export default Error;
